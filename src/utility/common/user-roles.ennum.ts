import { RoleEntity } from "../../roles/entities/role.entity";
import { UserEntity } from "../../users/entities/user.entity";

// 这里负责设置角色权限，admin表示超级管理员
export const Roles = {
  ADMIN: 'admin',
  USER: []
}